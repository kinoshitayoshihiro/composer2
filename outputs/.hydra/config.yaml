input_dim: 3
batch_size: 8
learning_rate: 0.001
num_workers: 0
data:
  train: /Users/kinoshitayoshihiro/music21/composer2-3/data/velocity.csv
  val: /Users/kinoshitayoshihiro/music21/composer2-3/data/velocity.csv
trainer:
  max_epochs: 2
  accelerator: cpu
  gpus: null
  devices: null
  default_root_dir: ${hydra.run.dir}
  logger:
    use_wandb: false
  callbacks:
    early_stopping:
      patience: 10
      monitor: val_MSE
      mode: min
      stopping_threshold: 0.015
  checkpoint_path: checkpoints/last.ckpt
training:
  epochs: 30
out:
  ckpt: ckpt_vscode.pt
device: mps
